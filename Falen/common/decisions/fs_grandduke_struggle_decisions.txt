fs_grandduke_dispatch_constables_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/decision_siege_warfare.dds" }
	decision_group_type = major
	desc = fs_grandduke_dispatch_constables_decision_desc
	selection_tooltip = fs_grandduke_dispatch_constables_decision_tooltip
	ai_check_interval = 24
	cooldown = { years = 10 }

	is_shown = {
		exists = struggle:grandduke_struggle
		has_title = title:k_n_grandduke
		struggle:grandduke_struggle = {
			is_struggle_phase = struggle_phase_grandduke_under_control
		}
	}

	is_valid = {
		is_capable_adult = yes
		is_imprisoned = no
		gold >= 300
	}

	cost = { gold = 300 }

	effect = {
		every_realm_county = { change_county_control = 20 }
		struggle:grandduke_struggle = {
			activate_struggle_catalyst = {
				catalyst = catalyst_grandduke_deescalation_50
				character = root
			}
		}
	}

	ai_potential = { always = yes }
	ai_will_do = { base = 25 }
}

fs_grandduke_religious_tolerance_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/decision_personal_religious.dds" }
	decision_group_type = major
	desc = fs_grandduke_religious_tolerance_decision_desc
	selection_tooltip = fs_grandduke_religious_tolerance_decision_tooltip
	ai_check_interval = 24
	cooldown = { years = 10 }

	is_shown = {
		exists = struggle:grandduke_struggle
		has_title = title:k_n_grandduke
		struggle:grandduke_struggle = {
			is_struggle_phase = struggle_phase_grandduke_under_control
		}
	}

	is_valid = {
		is_capable_adult = yes
		is_imprisoned = no
		prestige >= 750
	}

	cost = { prestige = 750 }

	effect = {
		every_realm_county = {
			add_county_modifier = {
				modifier = fs_grandduke_religious_tolerance_modifier
				years = 10
			}
		}
		struggle:grandduke_struggle = {
			activate_struggle_catalyst = {
				catalyst = catalyst_grandduke_deescalation_50
				character = root
			}
		}
	}

	ai_potential = { always = yes }
	ai_will_do = { base = 25 }
}

fs_grandduke_purge_heretics_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/decision_major_religion.dds" }
	decision_group_type = major
	desc = fs_grandduke_purge_heretics_decision_desc
	selection_tooltip = fs_grandduke_purge_heretics_decision_tooltip
	ai_check_interval = 24
	cooldown = { years = 10 }

	is_shown = {
		exists = struggle:grandduke_struggle
		has_title = title:k_n_grandduke
		struggle:grandduke_struggle = {
			is_struggle_phase = struggle_phase_grandduke_under_control
		}
	}

	is_valid = {
		is_capable_adult = yes
		is_imprisoned = no
		piety >= 500
	}

	cost = { piety = 500 }

	effect = {
		add_dread = 20
		struggle:grandduke_struggle = {
			activate_struggle_catalyst = {
				catalyst = catalyst_grandduke_deescalation_50
				character = root
			}
		}
	}

	ai_potential = { always = yes }
	ai_will_do = { base = 25 }
}

fs_grandduke_dispatch_imperial_army_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/decision_dynasty_house.dds" }
	decision_group_type = major
	desc = fs_grandduke_dispatch_imperial_army_decision_desc
	selection_tooltip = fs_grandduke_dispatch_imperial_army_decision_tooltip
	ai_check_interval = 24

	is_shown = {
		exists = struggle:grandduke_struggle
		has_title = title:k_n_grandduke
		NOT = { has_character_flag = fs_grandduke_dispatched_imperial_army }
		struggle:grandduke_struggle = {
			is_struggle_phase = struggle_phase_grandduke_under_control
		}
	}

	is_valid = {
		is_capable_adult = yes
		is_imprisoned = no
		exists = title:e_hec.holder
		title:e_hec.holder != root
	}

	effect = {
		add_character_flag = fs_grandduke_dispatched_imperial_army
		title:e_hec.holder = {
			add_hook = {
				type = favor_hook
				target = root
			}
		}
		add_tyranny = 20
		struggle:grandduke_struggle = {
			activate_struggle_catalyst = {
				catalyst = catalyst_grandduke_deescalation_200
				character = root
			}
		}
	}

	ai_potential = { always = yes }
	ai_will_do = { base = 10 }
}

fs_grandduke_end_struggle_decision = {
	picture = { reference = "gfx/interface/illustrations/decisions/decision_realm.dds" }
	decision_group_type = major
	desc = fs_grandduke_end_struggle_decision_desc
	selection_tooltip = fs_grandduke_end_struggle_decision_tooltip
	ai_check_interval = 12

	is_shown = {
		exists = struggle:grandduke_struggle
		has_title = title:k_n_grandduke
		struggle:grandduke_struggle = {
			is_struggle_phase = struggle_phase_grandduke_under_control
		}
	}

	is_valid = {
		is_capable_adult = yes
		is_imprisoned = no
		custom_description = {
			text = fs_grandduke_all_north_imperial_church_tt
			NOT = {
				any_county_in_region = {
					region = custom_grandduke
					NOT = { faith = faith:imperial_church }
				}
			}
		}
	}

	effect = {
		add_prestige = 3000
		add_piety = 2000
		add_realm_law_skip_effects = crown_authority_3
		set_global_variable = { name = fs_grandduke_struggle_resolved value = yes }
		remove_global_variable = fs_grandduke_struggle_annual_chain
		remove_global_variable = fs_grandduke_year_processed
		struggle:grandduke_struggle = {
			end_struggle = fs_grandduke_struggle_success_reason
		}
	}

	ai_potential = { always = yes }
	ai_will_do = { base = 100 }
}
